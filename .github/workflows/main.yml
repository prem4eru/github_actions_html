name:  Ec2 Deployment

on: 
  push:
    branches:
      - main

jobs: 
  deploy: 
    runs-on: ubuntu-latest
  

    steps: 
    - name: checking-out code 
      uses: actions/checkout@v4

    - name: Looking into runner machine 
      run: ls 
